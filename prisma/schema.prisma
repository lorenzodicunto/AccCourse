generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

// ─── Multi-Tenant RBAC Models ─────────────────────────────────────────────────
// All tables use "acc_" prefix via @@map to avoid conflicts with other projects
// sharing the same PostgreSQL database.

model Tenant {
  id        String   @id @default(cuid())
  name      String
  createdAt DateTime @default(now())
  users     User[]
  courses   Course[]
  assets    Asset[]

  @@map("acc_tenants")
}

model User {
  id           String   @id @default(cuid())
  email        String   @unique
  passwordHash String
  name         String
  role         String   @default("AUTHOR")
  tenantId     String?
  tenant       Tenant?  @relation(fields: [tenantId], references: [id])
  courses      Course[]
  assets       Asset[]    @relation("uploadedAssets")
  createdAt    DateTime @default(now())

  @@index([tenantId])
  @@map("acc_users")
}

model Course {
  id          String   @id @default(cuid())
  title       String
  description String   @default("")
  thumbnail   String   @default("")
  courseData   Json     @default("{}")
  tenantId    String?
  authorId    String
  tenant      Tenant?  @relation(fields: [tenantId], references: [id])
  author      User     @relation(fields: [authorId], references: [id])
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt

  @@index([tenantId])
  @@index([authorId])
  @@map("acc_courses")
}

// ─── Review Portal Models ─────────────────────────────────────────────────────

model SharedCourse {
  id         String    @id @default(cuid())
  title      String
  courseData  Json
  createdAt  DateTime  @default(now())
  comments   Comment[]

  @@map("acc_shared_courses")
}

model Reviewer {
  id        String    @id @default(cuid())
  name      String
  email     String
  createdAt DateTime  @default(now())
  comments  Comment[]

  @@map("acc_reviewers")
}

model Comment {
  id             String       @id @default(cuid())
  text           String
  slideId        String
  sharedCourseId String
  reviewerId     String
  createdAt      DateTime     @default(now())
  sharedCourse   SharedCourse @relation(fields: [sharedCourseId], references: [id], onDelete: Cascade)
  reviewer       Reviewer     @relation(fields: [reviewerId], references: [id], onDelete: Cascade)

  @@index([sharedCourseId])
  @@index([reviewerId])
  @@map("acc_comments")
}

// ─── Asset Library ────────────────────────────────────────────────────────────

model Asset {
  id         String   @id @default(cuid())
  name       String
  url        String
  type       String   // "image", "video", "document", "audio"
  size       Int      @default(0)
  tenantId   String?
  uploadedBy String
  tenant     Tenant?  @relation(fields: [tenantId], references: [id])
  uploader   User     @relation("uploadedAssets", fields: [uploadedBy], references: [id])
  createdAt  DateTime @default(now())

  @@index([tenantId])
  @@index([uploadedBy])
  @@map("acc_assets")
}
